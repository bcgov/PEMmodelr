#
#
#
# create_tiles <- function(trast, tile_size, out_dir ){
#
#   trast <- rtemplate
#   tile_size = 500
#   out_dir = tile_dir
#
#   makeTiles(trast, tile_size, filename = file.path(out_dir, "tile_.tif"), extend = T, na.rm = FALSE)
#
#
# }
